# K3s cluster token (shared secret between control plane and workers)
K3S_TOKEN=change-me

# Proxmox GPU resource mapping name
GPU_MAPPING=rtx3090

# Control plane hostname
CONTROL_PLANE_HOST=control-1

# SSH keys file path (on Proxmox host)
SSH_KEYS=/tmp/keys.pub
